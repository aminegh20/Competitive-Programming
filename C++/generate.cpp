#include <bits/stdc++.h>
using namespace std;

int main() {
  ios_base::sync_with_stdio(false);
  cin.tie(0);
  std::mt19937 rng(static_cast<unsigned int>(chrono::steady_clock().now().time_since_epoch().count()));
  freopen("input.txt", "w", stdout);

  fclose(stdout);
  return 0;
}